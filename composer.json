{
    "require": {
        "php": "^7.0.0",
        "ext-mbstring": "*"
    },
    "scripts": {
        "post-install-cmd": "cd api && composer install && php artisan migrate --force && php artisan db:seed --force"
    }
}

