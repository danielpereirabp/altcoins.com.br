{
    "name": "altcoins",
    "version": "1.0.0",
    "require": {
        "chalk": "*"
    },
    "cacheDirectories": [
        "web/node_modules"
    ],
    "scripts": {
        "postinstall": "npm install --production --prefix ./web && npm run build --prefix ./web"
    }
}
