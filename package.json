{
  "name": "altcoins",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "NODE_ENV=development npm install --prefix ./web && npm run build --prefix ./web"
  },
  "engines": {
    "node": "~8.10.0",
    "npm": "~5.6.0"
  }
}
